{
  "name": "all-editor",
  "version": "0.0.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build --prefix ./packages/core",
    "dev": "npm run dev --prefix ./packages/core",
    "demo": "npm run dev --prefix ./demo"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
